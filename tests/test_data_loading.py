"""Data-loading correctness: row counts, dtypes, primary-key uniqueness.

TODO(phase-1): assert Sackmann CSV row counts match DuckDB after ingestion,
assert no duplicate (source, match_external_id) rows, assert dates parse.
"""
